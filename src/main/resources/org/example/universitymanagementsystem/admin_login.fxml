<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.geometry.Pos?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.PasswordField?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.VBox?>

<BorderPane xmlns="http://javafx.com/javafx/21"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="org.example.universitymanagementsystem.controllers.AdminLoginController"
            style="-fx-background-color: linear-gradient(to bottom right, #eceff1, #ffffff);">

    <!-- ===== Top Title ===== -->
    <top>
        <VBox alignment="CENTER" spacing="10" BorderPane.alignment="CENTER">
            <padding>
                <Insets top="40" bottom="20"/>
            </padding>

            <Label text="Admin Login"
                   style="-fx-font-size: 28px; -fx-font-weight: bold; -fx-text-fill: #455a64; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 2, 0, 1, 1);"/>

            <Label text="Authorized access only"
                   style="-fx-text-fill: #78909c; -fx-font-size: 14px;"/>
        </VBox>
    </top>

    <!-- ===== Center Login Form ===== -->
    <center>
        <VBox alignment="CENTER" spacing="20"
              style="-fx-background-color: white; -fx-background-radius: 15; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.1), 10, 0, 0, 5); -fx-padding: 40;"
              maxWidth="400">

            <VBox spacing="5" alignment="CENTER_LEFT" maxWidth="300">
                <Label text="Username or Email"
                       style="-fx-font-size: 14px; -fx-text-fill: #546e7a; -fx-font-weight: bold;"/>
                <TextField fx:id="usernameField"
                           promptText="admin@university.edu"
                           prefWidth="300"
                           style="-fx-background-color: #f5f5f5; -fx-border-color: #cfd8dc; -fx-border-radius: 5; -fx-background-radius: 5; -fx-padding: 8;"/>
            </VBox>


            <VBox spacing="5" alignment="CENTER_LEFT" maxWidth="300">
                <Label text="Password"
                       style="-fx-font-size: 14px; -fx-text-fill: #546e7a; -fx-font-weight: bold;"/>
                <PasswordField fx:id="passwordField"
                               promptText="Enter your password"
                               prefWidth="300"
                               style="-fx-background-color: #f5f5f5; -fx-border-color: #cfd8dc; -fx-border-radius: 5; -fx-background-radius: 5; -fx-padding: 8;"/>
            </VBox>


            <Button text="Login"
                    prefWidth="300"
                    onAction="#handleLogin"
                    style="-fx-background-color: #455a64; -fx-text-fill: white; -fx-font-weight: bold; -fx-background-radius: 20; -fx-cursor: hand; -fx-padding: 10; -fx-font-size: 14px;"/>

            <Label fx:id="errorLabel"
                   text=""
                   style="-fx-text-fill: #d32f2f; -fx-font-size: 12px;"/>

        </VBox>
    </center>

    <!-- ===== Bottom ===== -->
    <bottom>
        <VBox alignment="CENTER">
            <padding>
                <Insets bottom="20" top="20"/>
            </padding>

            <Button text="← Back to Home"
                    style="-fx-background-color: transparent; -fx-text-fill: #546e7a; -fx-cursor: hand; -fx-underline: true;"
                    onAction="#goBack"/>
        </VBox>
    </bottom>

</BorderPane>

